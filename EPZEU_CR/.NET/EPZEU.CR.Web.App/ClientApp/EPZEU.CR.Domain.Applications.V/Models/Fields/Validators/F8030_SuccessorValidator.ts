import { RecordValidator } from 'EPZEU.CR.Domain';
import { F8030_Successor803 } from '../ModelsAutoGenerated';
import { CompanyValidator } from './CompanyValidator';

export class F8030_SuccessorValidator extends RecordValidator<F8030_Successor803> {
    constructor() {
        super();

        this.ruleFor(m => m.subject).setValidator(new CompanyValidator());
    }

    public validateInternal(obj: F8030_Successor803): boolean {
        let isValid = super.validateInternal(obj);

        if (!obj.subject.name) {
            isValid = false;
            obj.addError('subject', this.getMessage('CR_APP_00036_E'));  //Поле "Наименование" е задължително!
        }

        return isValid;
    }
}