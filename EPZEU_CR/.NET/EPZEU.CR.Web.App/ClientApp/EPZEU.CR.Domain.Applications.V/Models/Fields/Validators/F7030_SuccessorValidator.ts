import { RecordValidator } from 'EPZEU.CR.Domain';
import { F7030_Successor } from '../ModelsAutoGenerated';
import { CompanyWithBGNameValidator } from './CompanyWithBGNameValidator';

export class F7030_SuccessorValidator extends RecordValidator<F7030_Successor> {
    constructor() {
        super();
        this.ruleFor(m => m.subject).setValidator(new CompanyWithBGNameValidator());
    }

    public validateInternal(obj: F7030_Successor): boolean {
        let isValid = super.validateInternal(obj);

        return isValid;
    }
}