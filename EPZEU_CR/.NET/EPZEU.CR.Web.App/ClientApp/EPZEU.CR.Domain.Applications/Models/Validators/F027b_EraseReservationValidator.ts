import { ErrorLevels } from 'Cnsys.Core';
import { ProcessStates, RecordValidator } from 'EPZEU.CR.Domain';
import { F027b_EraseReservation } from '../ModelsAutoGenerated';

export class F027b_EraseReservationValidator extends RecordValidator<F027b_EraseReservation> {
    constructor() {
        super();
    }

    public validateInternal(obj: F027b_EraseReservation): boolean {
        let isValid = super.validateInternal(obj);

        if (!obj.cheked && this.validationContext.processStates == ProcessStates.ForChange) {
            obj.addError('cheked',this.getMessage('CR_APP_00026_E'), ErrorLevels.Error);
            return false;
        }

        return isValid;
    }
}