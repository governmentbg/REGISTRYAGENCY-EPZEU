import { TypeSystem } from 'Cnsys.Core'
import { ApplicationWithFieldsFormBase } from 'EPZEU.CR.Domain'
import { moduleContext } from '../../ModuleContext'
import { D1Fields } from '../ModelsAutoGenerated'

@TypeSystem.typeDecorator('D1', moduleContext.moduleName)
export class D1 extends ApplicationWithFieldsFormBase<D1Fields> {

    constructor(obj?: any) {
        super(obj)

        this.copyFrom(obj);

        if (obj && obj.fields) {
            this.fields = new D1Fields(obj.fields);
        }
    }
}
