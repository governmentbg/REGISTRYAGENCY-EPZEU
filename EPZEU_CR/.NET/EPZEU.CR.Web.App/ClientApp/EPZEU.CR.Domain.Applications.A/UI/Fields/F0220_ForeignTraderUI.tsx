import { EPZEUBaseComponent } from 'EPZEU.Core';
import { ListRecordsContainerProps, withListRecordsContainer } from 'EPZEU.CR.Domain';
import * as React from 'react';
import { F0220_ForeignTrader } from '../../Models/Fields/ModelsAutoGenerated';

class F0220_ForeignTraderUIImpl extends EPZEUBaseComponent<ListRecordsContainerProps, F0220_ForeignTrader> {

    renderEdit(): JSX.Element {
        return <div className="row">
            <div className="form-group col-12">{this.textBoxFor(x => x.text)}
            </div>
        </div>
    }

    renderDisplay(): JSX.Element {
        return <div><>{(this.model.text ? this.getResource('CR_GL_REPRESENTATIVE_L') + ": " : "") + this.model.text}</></div>
    }
}

export const F0220_ForeignTraderUI = withListRecordsContainer(F0220_ForeignTraderUIImpl, F0220_ForeignTrader, {
    addButtonLabelKey: "CR_APP_ADD_REPRESENTATIVE_L",
    hasAtLeastOneRecord: true,
    isMandatoryField: true,
})