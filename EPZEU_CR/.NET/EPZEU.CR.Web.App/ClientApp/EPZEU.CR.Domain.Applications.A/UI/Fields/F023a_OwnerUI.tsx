import { EPZEUBaseComponent, ValidationSummaryStrategy } from 'EPZEU.Core';
import { FieldContainerProps, PersonTypes, PersonUI, withFieldRecordContainer } from 'EPZEU.CR.Domain';
import * as React from "react";
import { F023a_Owner } from '../../Models/Fields/ModelsAutoGenerated';

class F023a_OwnerUI extends EPZEUBaseComponent<FieldContainerProps, F023a_Owner> {

    renderEdit(): JSX.Element {
        return (<>
            <PersonUI {...this.bind(m => m.subject)} PersonType={PersonTypes.Owner} />
        </>)
    }

    renderDisplay(): JSX.Element {
        return <PersonUI {...this.bind(m => m.subject)} PersonType={PersonTypes.Owner} />;
    }
}

export const F023a_OwnerFieldUI = withFieldRecordContainer(F023a_OwnerUI, {
    fieldLabelTextKey: "CR_F_23a_L",
    valSummaryStrategy: ValidationSummaryStrategy.excludeAllExcept,
    valSummaryRecursive: true,
    valSummaryPropNames: ["", "subject."]
});