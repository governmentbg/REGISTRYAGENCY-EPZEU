import { ObjectHelper } from 'Cnsys.Core';
import { EPZEUBaseComponent } from 'EPZEU.Core';
import { FieldContainerProps, withFieldRecordContainer } from 'EPZEU.CR.Domain';
import * as React from 'react';
import { F054a_VolumeOfRepresentationPower541 } from '../../Models/Fields/ModelsAutoGenerated';

class F054a_VolumeOfRepresentationPower541UI extends EPZEUBaseComponent<FieldContainerProps, F054a_VolumeOfRepresentationPower541> {

    renderEdit(): JSX.Element {
        return (
            <div className="row">
                <div className="form-group col-12">
                    {this.textAreaFor(x => x.text, null, 3)}
                </div>
            </div>
        );
    }

    renderDisplay(): JSX.Element {
        return (!ObjectHelper.isStringNullOrEmpty(this.model.text) ? <span className="preserve-line-breaks">{this.model.text}</span> : null)
    }
}

export const F054a_VolumeOfRepresentationPower541FieldUI = withFieldRecordContainer(F054a_VolumeOfRepresentationPower541UI, {
    fieldLabelTextKey: "CR_F_54_L",
    fieldLabelFor: (model: F054a_VolumeOfRepresentationPower541) => model.text
});