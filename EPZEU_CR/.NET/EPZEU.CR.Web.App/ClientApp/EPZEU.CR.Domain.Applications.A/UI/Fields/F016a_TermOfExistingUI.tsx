import { EPZEUBaseComponent } from 'EPZEU.Core';
import { FieldContainerProps, withFieldRecordContainer } from 'EPZEU.CR.Domain';
import * as React from "react";
import { F016a_TermOfExisting } from '../../Models/Fields/ModelsAutoGenerated';

interface F016_TermsOfPartnershipProps extends FieldContainerProps {
    showTermType?: boolean;
}

class F016a_TermOfExistingUI extends EPZEUBaseComponent<F016_TermsOfPartnershipProps, F016a_TermOfExisting> {

    renderEdit(): JSX.Element {
        return (<div className="row">
            <div className="form-group col-sm-6">
                {this.dateFor(x => x.date)}
            </div>
        </div>)
    }

    renderDisplay(): JSX.Element {
        return (<>{this.model.date && <>{this.dateDisplayFor(this.model.date, null, this.getResource('GL_YEAR_ABBREVIATION_L'))}</>}</>);
    }
}

export const F016a_TermOfExistingFieldUI = withFieldRecordContainer(F016a_TermOfExistingUI, {
    fieldLabelTextKey: "CR_F_16a_L",
    fieldLabelFor: (model: F016a_TermOfExisting) => model.date
});