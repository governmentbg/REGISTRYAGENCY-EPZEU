import * as React from "react";
import { EPZEUBaseComponent } from 'EPZEU.Core'
import { F03110_CredentialsForDifferentTypes } from '../../Models/Fields/ModelsAutoGenerated'
import { withRecordContainer, RecordContainerProps } from 'EPZEU.CR.Domain';

class F03110_CredentialsForDifferentTypesUIImpl extends EPZEUBaseComponent<RecordContainerProps, F03110_CredentialsForDifferentTypes> {

    renderEdit(): JSX.Element {
        return (<div className="row">
            <div className="form-group col-sm-12">
                {this.labelFor(m => m.text, 'CR_GL_RIFHTS_CLASSES_SHARES_L')}
                {this.textAreaFor(m => m.text, null, 3)}
            </div>
        </div>)
    }

    renderDisplay(): JSX.Element {
        return (this.model.text ? <span className="preserve-line-breaks">{this.getResource('CR_GL_RIFHTS_CLASSES_SHARES_L')}: {this.model.text}</span> : null);
    }
}

export const F03110_CredentialsForDifferentTypesUI = withRecordContainer(F03110_CredentialsForDifferentTypesUIImpl);