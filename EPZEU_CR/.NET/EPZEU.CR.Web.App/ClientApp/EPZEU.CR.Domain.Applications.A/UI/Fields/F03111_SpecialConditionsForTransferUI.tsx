import * as React from "react";
import { EPZEUBaseComponent } from 'EPZEU.Core'
import { F03111_SpecialConditionsForTransfer } from '../../Models/Fields/ModelsAutoGenerated'
import { withFieldRecordContainer, FieldContainerProps } from 'EPZEU.CR.Domain';

class F03111_SpecialConditionsForTransferUI extends EPZEUBaseComponent<FieldContainerProps, F03111_SpecialConditionsForTransfer> {

    constructor(props?: FieldContainerProps) {
        super(props);
    }

    renderEdit(): JSX.Element {
        return (<div className="row">
            <div className="form-group col-sm-12">
                {this.labelFor(m => m.text, 'CR_GL_CONDITIONS_TRANSFER_SHARE_L')}
                {this.textAreaFor(m => m.text, null, 3)}
            </div>
        </div>)
    }

    renderDisplay(): JSX.Element {
        return (this.model.text ? <span className="preserve-line-breaks">{this.getResource('CR_GL_CONDITIONS_TRANSFER_SHARE_L')}: {this.model.text}</span> : null);
    }
}

export const F03111_SpecialConditionsForTransferFieldUI = withFieldRecordContainer(F03111_SpecialConditionsForTransferUI, {});