import { EPZEUBaseValidator } from 'EPZEU.Core';
import { F017a_DesignatedToPerformPublicBenefit } from '../ModelsAutoGenerated';

//Наследява EPZEUBaseValidator, а не RecordValidator-a, защото не искаме да се валидира при промяна за "празно или променено поле не може да бъде изтрито" - TRIR-5235
export class F017a_DesignatedToPerformPublicBenefitValidator extends EPZEUBaseValidator<F017a_DesignatedToPerformPublicBenefit, any> {
    constructor() {
        super();
    }

    public validate(obj: F017a_DesignatedToPerformPublicBenefit): boolean {
        let isValid = super.validate(obj);

        return isValid;
    }
}