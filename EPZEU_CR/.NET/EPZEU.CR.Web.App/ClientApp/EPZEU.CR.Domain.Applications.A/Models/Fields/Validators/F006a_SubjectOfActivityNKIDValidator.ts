import { ApplicationFormTypes } from 'EPZEU.CR.Core';
import { RecordValidator } from 'EPZEU.CR.Domain';
import { F006a_SubjectOfActivityNKID } from '../ModelsAutoGenerated';

export class F006a_SubjectOfActivityNKIDValidator extends RecordValidator<F006a_SubjectOfActivityNKID> {
    constructor() {
        super();
    }

    public validateInternal(obj: F006a_SubjectOfActivityNKID): boolean {
        if (this.validationContext.appType != ApplicationFormTypes.A11
            && this.validationContext.appType != ApplicationFormTypes.A14
            && this.validationContext.appType != ApplicationFormTypes.A17) {

            let isValid = super.validateInternal(obj);

            return isValid;
        } else {
            return true;
        }
    }
}