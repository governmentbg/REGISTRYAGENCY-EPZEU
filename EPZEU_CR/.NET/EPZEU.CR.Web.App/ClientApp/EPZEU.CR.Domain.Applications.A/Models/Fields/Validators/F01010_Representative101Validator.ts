import { RecordValidator, SubjectFLEWithCountyValidator } from 'EPZEU.CR.Domain';
import { F01010_Representative101 } from '../ModelsAutoGenerated';

export class F01010_Representative101Validator extends RecordValidator<F01010_Representative101> {
    constructor() {
        super();

        this.ruleFor(m => m.subject).setValidator(new SubjectFLEWithCountyValidator());
    }

    public validateInternal(obj: F01010_Representative101): boolean {
        let isValid = super.validateInternal(obj);

        return isValid;
    }
}