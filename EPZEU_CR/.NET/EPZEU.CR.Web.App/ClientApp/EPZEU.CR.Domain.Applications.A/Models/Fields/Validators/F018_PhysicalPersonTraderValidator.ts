import { PersonWithCountyValidator, RecordValidator } from 'EPZEU.CR.Domain';
import { F018_PhysicalPersonTrader } from '../ModelsAutoGenerated';

export class F018_PhysicalPersonTraderValidator extends RecordValidator<F018_PhysicalPersonTrader> {

    constructor() {
        super();

        this.ruleFor(m => m.person).setValidator(new PersonWithCountyValidator());
    }

    public validateInternal(obj: F018_PhysicalPersonTrader): boolean {

        let isValid = super.validateInternal(obj);

        return isValid;
    }
}