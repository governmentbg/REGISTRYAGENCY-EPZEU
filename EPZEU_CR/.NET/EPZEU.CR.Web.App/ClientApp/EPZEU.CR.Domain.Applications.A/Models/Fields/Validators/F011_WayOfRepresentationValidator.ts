import { RecordValidator } from 'EPZEU.CR.Domain';
import { F011_WayOfRepresentation } from '../ModelsAutoGenerated';

export class F011_WayOfRepresentationValidator extends RecordValidator<F011_WayOfRepresentation> {
    constructor() {
        super();
        //Когато е избран друг начин на представляване трябва да се попълни описателното поле.
        this.ruleFor(m => m.text).notEmpty().when(m => m.otherWay).withMessage(this.getMessage('CR_APP_00046_E'));
    }

    public validateInternal(obj: F011_WayOfRepresentation): boolean {
        let isValid = super.validateInternal(obj);

        return isValid;
    }
}