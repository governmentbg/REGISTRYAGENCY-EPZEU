import { ApplicationFormTypes } from 'EPZEU.CR.Core';
import { RecordValidator } from 'EPZEU.CR.Domain';
import { F054a_VolumeOfRepresentationPower541 } from '../ModelsAutoGenerated';

export class F054a_VolumeOfRepresentationPower541Validator extends RecordValidator<F054a_VolumeOfRepresentationPower541> {
    constructor() {
        super();

        this.ruleFor(m => m.text).notEmpty().withMessage(this.getMessage('GL_INPUT_FIELD_MUST_E'));
    }

    public validateInternal(obj: F054a_VolumeOfRepresentationPower541): boolean {

        if (this.validationContext.appType == ApplicationFormTypes.A18) {
            obj.clearErrors(true);

            return true
        } else
            return super.validateInternal(obj);
    }
}