import { RecordValidator } from 'EPZEU.CR.Domain';
import { F309_InterestAndDefaultForDelay } from '../ModelsAutoGenerated';

export class F309_InterestAndDefaultForDelayValidator extends RecordValidator<F309_InterestAndDefaultForDelay> {

    constructor() {
        super();
    }

    public validateInternal(obj: F309_InterestAndDefaultForDelay): boolean {
        let isValid = super.validateInternal(obj);

        return isValid;
    }
}