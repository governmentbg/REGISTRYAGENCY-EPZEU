import { RecordValidator } from 'EPZEU.CR.Domain';
import { F314a_PledgedCreditorAgreement2 } from '../ModelsAutoGenerated';

export class F314a_PledgedCreditorAgreement2Validator extends RecordValidator<F314a_PledgedCreditorAgreement2> {
    constructor() {
        super();
    }

    public validateInternal(obj: F314a_PledgedCreditorAgreement2): boolean {
        let isValid = super.validateInternal(obj);

        return isValid;
    }
}