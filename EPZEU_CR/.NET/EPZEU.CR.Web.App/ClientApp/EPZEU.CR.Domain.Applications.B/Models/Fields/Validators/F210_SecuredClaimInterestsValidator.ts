import { RecordValidator } from 'EPZEU.CR.Domain';
import { F210_SecuredClaimInterests } from '../ModelsAutoGenerated';

export class F210_SecuredClaimInterestsValidator extends RecordValidator<F210_SecuredClaimInterests> {

    public validateInternal(obj: F210_SecuredClaimInterests): boolean {
        let isValid = super.validateInternal(obj);

        return isValid;
    }
}