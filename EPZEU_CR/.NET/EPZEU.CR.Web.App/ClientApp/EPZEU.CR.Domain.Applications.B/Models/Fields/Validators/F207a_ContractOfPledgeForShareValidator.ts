import { ObjectHelper } from 'Cnsys.Core';
import { RecordValidator, ValidatorHelpers } from 'EPZEU.CR.Domain';
import { F207a_ContractOfPledgeForShare } from '../ModelsAutoGenerated';

export class F207a_ContractOfPledgeForShareValidator extends RecordValidator<F207a_ContractOfPledgeForShare> {
    constructor() {
        super();

        let errMsg = this.getMessage('GL_INPUT_DATE_E');
        this.ruleFor(m => m.date).must(m => ValidatorHelpers.isValidDate(m.date)).withMessage(errMsg).when(m => !ObjectHelper.isStringNullOrEmpty(m.date));
    }

    public validateInternal(obj: F207a_ContractOfPledgeForShare): boolean {
        let isValid = super.validateInternal(obj);

        return isValid;
    }
}