import { RecordValidator } from 'EPZEU.CR.Domain';
import { F222_StopOfExecutionSize } from '../ModelsAutoGenerated';

export class F222_StopOfExecutionSizeValidator extends RecordValidator<F222_StopOfExecutionSize> {

    public validateInternal(obj: F222_StopOfExecutionSize): boolean {
        let isValid = super.validateInternal(obj);

        return isValid;
    }
}