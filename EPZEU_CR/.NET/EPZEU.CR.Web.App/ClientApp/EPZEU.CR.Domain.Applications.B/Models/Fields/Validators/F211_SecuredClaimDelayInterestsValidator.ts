import { RecordValidator } from 'EPZEU.CR.Domain';
import { F211_SecuredClaimDelayInterests } from '../ModelsAutoGenerated';

export class F211_SecuredClaimDelayInterestsValidator extends RecordValidator<F211_SecuredClaimDelayInterests> {

    public validateInternal(obj: F211_SecuredClaimDelayInterests): boolean {
        let isValid = super.validateInternal(obj);

        return isValid;
    }
}