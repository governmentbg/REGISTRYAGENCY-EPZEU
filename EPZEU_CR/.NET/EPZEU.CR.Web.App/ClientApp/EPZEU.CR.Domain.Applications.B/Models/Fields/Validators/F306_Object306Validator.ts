import { RecordValidator } from 'EPZEU.CR.Domain';
import { F306_Object306 } from '../ModelsAutoGenerated';

export class F306_Object306Validator extends RecordValidator<F306_Object306> {

    constructor() {
        super();
    }

    public validateInternal(obj: F306_Object306): boolean {
        let isValid = super.validateInternal(obj);

        if (!obj.forGivingAmount && !obj.forGive && !obj.forDoingActivity && !obj.forNotDoingActivity) {
            obj.addError(this.getMessage('CR_APP_00139_E'))
                isValid = false;
        }

        return isValid;
    }
}