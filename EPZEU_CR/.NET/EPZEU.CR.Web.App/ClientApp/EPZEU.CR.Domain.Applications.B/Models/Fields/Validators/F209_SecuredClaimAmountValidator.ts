import { RecordValidator } from 'EPZEU.CR.Domain';
import { F209_SecuredClaimAmount } from '../ModelsAutoGenerated';

export class F209_SecuredClaimAmountValidator extends RecordValidator<F209_SecuredClaimAmount> {

    public validateInternal(obj: F209_SecuredClaimAmount): boolean {
        let isValid = super.validateInternal(obj);

        return isValid;
    }
}