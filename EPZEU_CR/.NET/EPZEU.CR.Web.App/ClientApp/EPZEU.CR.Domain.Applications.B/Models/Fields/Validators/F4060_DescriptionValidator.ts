import { RecordValidator, SubjectFLEWithCountyValidator } from 'EPZEU.CR.Domain';
import { F040601_Description406 } from '../ModelsAutoGenerated';

export class F4060_DescriptionValidator extends RecordValidator<F040601_Description406> {
    constructor() {
        super();

        this.ruleFor(m => m.subject).setValidator(new SubjectFLEWithCountyValidator());
    }

    public validateInternal(obj: F040601_Description406): boolean {
        let isValid = super.validateInternal(obj);

        return isValid;
    }
}