import { RecordValidator } from 'EPZEU.CR.Domain';
import { F312_Price312 } from '../ModelsAutoGenerated';

export class F312_Price312Validator extends RecordValidator<F312_Price312> {

    constructor() {
        super();
    }

    public validateInternal(obj: F312_Price312): boolean {
        let isValid = super.validateInternal(obj);

        return isValid;
    }
}