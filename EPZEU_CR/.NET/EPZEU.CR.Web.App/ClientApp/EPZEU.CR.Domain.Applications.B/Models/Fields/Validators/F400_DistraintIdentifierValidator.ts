import { RecordValidator } from 'EPZEU.CR.Domain';
import { F400_DistraintIdentifier } from '../ModelsAutoGenerated';

export class F400_DistraintIdentifierValidator extends RecordValidator<F400_DistraintIdentifier> {
    constructor() {
        super();
    }

    public validateInternal(obj: F400_DistraintIdentifier): boolean {
        let isValid = super.validateInternal(obj);

        return isValid;
    }
}