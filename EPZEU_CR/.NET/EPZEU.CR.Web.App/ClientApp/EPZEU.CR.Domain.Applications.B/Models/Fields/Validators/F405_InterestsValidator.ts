import { RecordValidator } from 'EPZEU.CR.Domain';
import { F405_Interests } from '../../Fields/ModelsAutoGenerated';

export class F405_InterestsValidator extends RecordValidator<F405_Interests> {
    constructor() {
        super();
    }

    public validateInternal(obj: F405_Interests): boolean {
        let isValid = super.validateInternal(obj);

        if (!obj.text) {
            isValid = false;
            obj.addError(this.getMessage('GL_INPUT_FIELD_MUST_E')); //Полето е задължително
        }

        return isValid;
    }
}