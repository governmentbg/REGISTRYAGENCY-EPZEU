import { RecordValidator } from 'EPZEU.CR.Domain';
import { F531_OffshoreCompany } from '../ModelsAutoGenerated';

export class F531_OffshoreCompanyValidator extends RecordValidator<F531_OffshoreCompany> {
    constructor() {
        super();

        //Грешката трябва да се визуализира при невалидна стойност в полето 
        this.ruleFor(m => m.text).notEmpty().withMessage(this.getMessage('GL_INPUT_FIELD_MUST_E'));
        //Полето трябва да е изписано на кирилица. 
        this.ruleFor(m => m.text).matches('^(?:(?![ЫЭыэ])[А-Яа-я0-9-~@#$%^&*()_+{}|":><.,/?\';=|!\\\\ ])+$').withMessage(this.getMessage('GL_INPUT_CIRILIC_MUST_E'));
    }

    public validateInternal(obj: F531_OffshoreCompany): boolean {
        let isValid = super.validateInternal(obj);

        return isValid;
    }
}