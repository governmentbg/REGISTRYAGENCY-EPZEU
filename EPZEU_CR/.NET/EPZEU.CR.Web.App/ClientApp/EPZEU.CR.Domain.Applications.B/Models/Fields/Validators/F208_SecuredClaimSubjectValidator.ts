import { RecordValidator } from 'EPZEU.CR.Domain';
import { F208_SecuredClaimSubject } from '../ModelsAutoGenerated';

export class F208_SecuredClaimSubjectValidator extends RecordValidator<F208_SecuredClaimSubject> {

    public validateInternal(obj: F208_SecuredClaimSubject): boolean {
        let isValid = super.validateInternal(obj);

        if (!obj.doingActions && !obj.givingMoney && !obj.givingThing && !obj.notDoingActions) {
            obj.addError(this.getMessage('CR_APP_00139_E')); //Изберете "Предмет"!

            isValid = false;
        }

        return isValid;
    }
}