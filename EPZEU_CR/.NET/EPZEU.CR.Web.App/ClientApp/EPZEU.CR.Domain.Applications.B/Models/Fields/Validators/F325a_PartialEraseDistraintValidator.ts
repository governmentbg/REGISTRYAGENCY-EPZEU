import { RecordValidator } from 'EPZEU.CR.Domain';
import { F325a_PartialEraseDistraint } from '../ModelsAutoGenerated';

export class F325a_PartialEraseDistraintValidator extends RecordValidator<F325a_PartialEraseDistraint> {

    constructor() {
        super();
    }

    public validateInternal(obj: F325a_PartialEraseDistraint): boolean {
        let isValid = super.validateInternal(obj);

        return isValid;
    }
}