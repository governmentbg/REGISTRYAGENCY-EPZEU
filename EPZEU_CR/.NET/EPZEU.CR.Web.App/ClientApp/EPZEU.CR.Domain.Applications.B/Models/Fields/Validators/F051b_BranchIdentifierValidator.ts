import { ObjectHelper } from 'Cnsys.Core';
import { ProcessStates, RecordValidator } from 'EPZEU.CR.Domain';
import { F051b_BranchIdentifier } from '../ModelsAutoGenerated';

export class F051b_BranchIdentifierValidator extends RecordValidator<F051b_BranchIdentifier> {

    public validateInternal(obj: F051b_BranchIdentifier): boolean {
        let isValid = super.validateInternal(obj);

        if (this.validationContext.processStates == ProcessStates.Preregistration) {
            if (ObjectHelper.isStringNullOrEmpty(obj.text)) {
                isValid = false;
                obj.addError("text", this.getMessage('GL_INPUT_FIELD_MUST_E'));
            }
        }

        return isValid;
    }
}