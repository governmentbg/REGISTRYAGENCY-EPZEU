import { RecordValidator } from 'EPZEU.CR.Domain';
import { F214_PledgePropertyPrice } from '../ModelsAutoGenerated';

export class F214_PledgePropertyPriceValidator extends RecordValidator<F214_PledgePropertyPrice> {

    public validateInternal(obj: F214_PledgePropertyPrice): boolean {
        let isValid = super.validateInternal(obj);

        return isValid;
    }
}