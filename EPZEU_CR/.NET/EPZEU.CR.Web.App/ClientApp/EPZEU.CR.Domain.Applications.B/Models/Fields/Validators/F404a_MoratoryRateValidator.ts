import { PriceValidator, RecordValidator } from 'EPZEU.CR.Domain';
import { F404a_MoratoryRate } from '../../Fields/ModelsAutoGenerated';

export class F404a_MoratoryRateValidator extends RecordValidator<F404a_MoratoryRate> {
    constructor() {
        super();
        this.ruleFor(m => m.date).isValidDate().withMessage(this.getMessage('GL_INPUT_DATE_E')); //  Въведете дата във формат "ДД.ММ.ГГГГ".
        this.ruleFor(m => m.price).setValidator(new PriceValidator());
    }

    public validateInternal(obj: F404a_MoratoryRate): boolean {
        let isValid = super.validateInternal(obj);

        return isValid;
    }
}