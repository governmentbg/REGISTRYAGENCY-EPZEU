import { RecordValidator } from 'EPZEU.CR.Domain';
import { F221_DepozitarDistraintRemove } from '../ModelsAutoGenerated';

export class F221_DepozitarDistraintRemoveValidator extends RecordValidator<F221_DepozitarDistraintRemove> {

    public validateInternal(obj: F221_DepozitarDistraintRemove): boolean {
        let isValid = super.validateInternal(obj);

        return isValid;
    }
}