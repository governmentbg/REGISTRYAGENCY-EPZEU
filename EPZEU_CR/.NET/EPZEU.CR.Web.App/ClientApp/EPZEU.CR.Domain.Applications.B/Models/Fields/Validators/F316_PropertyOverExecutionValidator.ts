import { RecordValidator } from 'EPZEU.CR.Domain';
import { F316_PropertyOverExecution } from '../ModelsAutoGenerated';

export class F316_PropertyOverExecutionValidator extends RecordValidator<F316_PropertyOverExecution> {

    constructor() {
        super();
    }

    public validateInternal(obj: F316_PropertyOverExecution): boolean {
        let isValid = super.validateInternal(obj);

        return isValid;
    }
}