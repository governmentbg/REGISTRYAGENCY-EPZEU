import { RecordValidator } from 'EPZEU.CR.Domain';
import { F551_EraseActualOwner } from '../ModelsAutoGenerated';

export class F551_EraseActualOwnerValidator extends RecordValidator<F551_EraseActualOwner> {
    constructor() {
        super();
    }

    public validateInternal(obj: F551_EraseActualOwner): boolean {
        let isValid = super.validateInternal(obj);

        return isValid;
    }
}