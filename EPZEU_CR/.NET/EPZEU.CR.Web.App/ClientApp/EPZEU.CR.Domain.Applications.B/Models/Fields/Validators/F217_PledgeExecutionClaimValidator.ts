import { RecordValidator } from 'EPZEU.CR.Domain';
import { F217_PledgeExecutionClaim } from '../ModelsAutoGenerated';

export class F217_PledgeExecutionClaimValidator extends RecordValidator<F217_PledgeExecutionClaim> {

    public validateInternal(obj: F217_PledgeExecutionClaim): boolean {
        let isValid = super.validateInternal(obj);

        return isValid;
    }
}