import { EPZEUBaseComponent } from 'EPZEU.Core';
import { FieldContainerProps, withFieldRecordContainer } from 'EPZEU.CR.Domain';
import * as React from "react";
import { F501_TermsOfLiquidation } from '../../Models/Fields/ModelsAutoGenerated';

class F501_TermsOfLiquidationUI extends EPZEUBaseComponent<FieldContainerProps, F501_TermsOfLiquidation> {

    renderEdit(): JSX.Element {
        return (<div className="row">
            <div className="form-group col-sm-6">
                {this.labelFor(m => m.date, 'GL_DATE_L')}
                {this.dateFor(x => x.date)}
            </div>
            <div className="form-group col-sm-12">
                {this.labelFor(m => m.termType, 'CR_APP_WAY_TERM_OF_COMPANY_SET_L')}
                {this.textAreaFor(m => m.termType, null, 3)}
            </div>
        </div>)
    }

    renderDisplay(): JSX.Element {
        return (<>{this.model.date && <>{this.dateDisplayFor(this.model.date, null, this.getResource('GL_YEAR_ABBREVIATION_L'))}</>}
            {this.model.date && this.model.termType && <>< br /></>}
            {this.model.termType && <>{this.getResource('CR_APP_WAY_DETERMINATE_TERM_L')}: {this.model.termType} </>}
        </>); 
    }
}

export const F501_TermsOfLiquidationFieldUI = withFieldRecordContainer(F501_TermsOfLiquidationUI, {
    fieldLabelTextKey: "CR_F_501_L",
    fieldLabelFor: (model: F501_TermsOfLiquidation) => model.date,
    isMandatoryField: true
});