import { ObjectHelper } from "Cnsys.Core";
import { EPZEUBaseComponent } from 'EPZEU.Core';
import { FieldContainerProps, withFieldRecordContainer } from 'EPZEU.CR.Domain';
import * as React from "react";
import { F054_VolumeOfRepresentationPower } from '../../Models/Fields/ModelsAutoGenerated';

class F054_VolumeOfRepresentationPowerUI extends EPZEUBaseComponent<FieldContainerProps, F054_VolumeOfRepresentationPower> {

    constructor(props: any) {
        super(props);

    }

    renderEdit(): JSX.Element {
        return (
            <div className="row">
                <div className="form-group col-12">
                    {this.textAreaFor(x => x.text, null, 3)}
                </div>
            </div>
        );
    }

    renderDisplay(): JSX.Element {
        return (!ObjectHelper.isStringNullOrEmpty(this.model.text) ? <span className="preserve-line-breaks">{this.model.text}</span> : null);
    }
}

export const F054_VolumeOfRepresentationPowerFieldUI = withFieldRecordContainer(F054_VolumeOfRepresentationPowerUI,
    {
        fieldLabelTextKey: "CR_F_54_L",
        isMandatoryField: true,
        fieldLabelFor: (model: F054_VolumeOfRepresentationPower) => model.text
    });