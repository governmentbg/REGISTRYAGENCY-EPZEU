import { ObjectHelper } from 'Cnsys.Core';
import { EPZEUBaseComponent } from 'EPZEU.Core';
import { FieldContainerProps, withFieldRecordContainer } from 'EPZEU.CR.Domain';
import { observer } from 'mobx-react';
import * as React from "react";
import { F207a_ContractOfPledgeForShare } from '../../Models/Fields/ModelsAutoGenerated';

@observer class F207a_ContractOfPledgeForShareUI extends EPZEUBaseComponent<FieldContainerProps, F207a_ContractOfPledgeForShare> {
    renderEdit(): JSX.Element {
        let result: any = null;

        if (this.model) {
            result = (
                <>

                    <div className="row">
                        <div className="form-group col-sm-6">
                            {this.labelFor(x => x.date,"GL_DATE_L")}
                            {this.dateFor(x => x.date)}
                        </div>
                    </div>
                </>);
        }

        return result;
    }

    renderDisplay(): JSX.Element {
        return <>
            <div>{this.model.date && <>{this.getResource('GL_DATE_L') + ": " + this.dateDisplayFor(this.model.date, null, this.getResource('GL_YEAR_ABBREVIATION_L'))}</>}</div>
        </>
    }
}

export const F207a_ContractOfPledgeForShareFieldUI = withFieldRecordContainer(F207a_ContractOfPledgeForShareUI, {
    fieldLabelTextKey: "CR_F_207a_L",
});