import { ObjectHelper } from "Cnsys.Core";
import { EPZEUBaseComponent } from 'EPZEU.Core';
import { FieldContainerProps, withFieldRecordContainer } from 'EPZEU.CR.Domain';
import * as React from "react";
import { F223_StopOfExecutionProperty } from '../../Models/Fields/ModelsAutoGenerated';

class F223_StopOfExecutionPropertyUI extends EPZEUBaseComponent<FieldContainerProps, F223_StopOfExecutionProperty> {

    renderEdit(): JSX.Element {
        return <div className="row">
            <div className="form-group col-sm-12">
                {this.textAreaFor(m => m.text, null, 3)}
            </div>
        </div>
    }

    renderDisplay(): JSX.Element {
        return (<div>
            {!ObjectHelper.isArrayNullOrEmpty(this.model.text) ? this.model.text : null}
        </div>);
    }
}

export const F223_StopOfExecutionPropertyFieldUI = withFieldRecordContainer(F223_StopOfExecutionPropertyUI, {
    fieldLabelTextKey: "CR_F_223_L",
});
