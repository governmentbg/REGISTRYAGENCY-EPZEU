import { ObjectHelper } from "Cnsys.Core";
import { EPZEUBaseComponent } from 'EPZEU.Core';
import { FieldContainerProps, withFieldRecordContainer } from 'EPZEU.CR.Domain';
import * as React from "react";
import { F222_StopOfExecutionSize } from '../../Models/Fields/ModelsAutoGenerated';

class F222_StopOfExecutionSizeUI extends EPZEUBaseComponent<FieldContainerProps, F222_StopOfExecutionSize> {

    renderEdit(): JSX.Element {
        return <div className="row">
            <div className="form-group col-sm-6 col-lg-4">
                {this.labelFor(x => x.price.units, "CR_APP_SIZE_L")}
                {this.textBoxFor(x => x.price.amount)}
            </div>
            <div className="form-group col-sm-6 col-lg-4">
                {this.labelFor(x => x.price.units, "CR_APP_UNITS_L")}
                {this.textBoxFor(x => x.price.units)}
            </div>
        </div>
    }

    renderDisplay(): JSX.Element {
        return (<div>
            <div>
                {!ObjectHelper.isArrayNullOrEmpty(this.model.price.amount) ? <>{this.model.price.amount + " "}</> : null}
                {!ObjectHelper.isArrayNullOrEmpty(this.model.price.units) ? <>{this.model.price.units}</> : null}
            </div>
        </div>);
    }
}

export const F222_StopOfExecutionSizeFieldUI = withFieldRecordContainer(F222_StopOfExecutionSizeUI, {
    fieldLabelTextKey: "CR_F_222_L",
});