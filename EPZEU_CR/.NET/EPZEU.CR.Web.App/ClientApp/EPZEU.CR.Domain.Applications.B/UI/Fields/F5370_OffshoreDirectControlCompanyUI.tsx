import { ObjectHelper } from 'Cnsys.Core';
import { EPZEUBaseComponent } from 'EPZEU.Core';
import { FieldContainerProps, withFieldRecordContainer } from 'EPZEU.CR.Domain';
import * as React from 'react';
import { F5370_OffshoreDirectControlCompany } from '../../Models/Fields/ModelsAutoGenerated';

class F5370_OffshoreDirectControlCompanyUIImpl extends EPZEUBaseComponent<FieldContainerProps, F5370_OffshoreDirectControlCompany> {

    renderEdit(): JSX.Element {
        return <>
            <div className="row">
                <div className="form-group col-12">
                    <label>{this.getResource("CR_GL_COMPANY_NAME_L")}</label>
                    {this.textBoxFor(x => x.name)}
                </div>
            </div>
        </>
    }

    renderDisplay(): JSX.Element {

        if (!ObjectHelper.isStringNullOrEmpty(this.model.name))
            return <>{`${this.getResource("CR_GL_COMPANY_NAME_L")}: ${this.model.name}` }</>

        return null;
    }
}

export const F5370_OffshoreDirectControlCompanyFieldUI = withFieldRecordContainer(F5370_OffshoreDirectControlCompanyUIImpl, {
    fieldLabelFor: (model: F5370_OffshoreDirectControlCompany) => model.name
});