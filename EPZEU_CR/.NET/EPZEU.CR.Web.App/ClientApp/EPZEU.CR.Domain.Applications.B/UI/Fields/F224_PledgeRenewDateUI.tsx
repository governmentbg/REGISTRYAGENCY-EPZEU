import { EPZEUBaseComponent, attributesClassControlLabel } from 'EPZEU.Core';
import { FieldContainerProps, withFieldRecordContainer } from 'EPZEU.CR.Domain';
import * as React from "react";
import { F224_PledgeRenewDate } from '../../Models/Fields/ModelsAutoGenerated';
import * as Datetime from 'react-datetime';


{/*pri polzvane na textBoxFor vmesto dateFor() v promqna na obstoqtelstva ne se formatira datata zashtoto e TextBox za poleta 224 i 326*/ }
{/*problem s bazova kontrola za data pri podavane na klasove kato atribut ne formatira pravilno poletata - svurzano s bug nomer: TRIR-3095*/ }
{/*this.dateFor(x => x.date, null,null,  { className: 'form-control', disabled:true })*/ }

const attributesClassFieldTittlePreview = { className: 'disabled' };

class F224_PledgeRenewDateUI extends EPZEUBaseComponent<FieldContainerProps, F224_PledgeRenewDate> {

    renderEdit(): JSX.Element {
        return (
            <>
                <div className="row">
                    <div className="form-group col-12">
                        <div className="input-group date-control">
                            <Datetime inputProps={{ disabled: true }} value={this.model.date} />
                            <div className="input-group-append">
                                <button className="btn btn-secondary" type="button">
                                    <i className="ui-icon ui-icon-calendar"></i>
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
                <div className="row">
                    <div className="form-group col-sm-12">
                        <div className="custom-control-inline custom-control custom-checkbox">
                            {this.checkBoxFor(x => x.processingRenewDistraintRequired, "CR_APP_RENEWAL_ENTRY_L")}
                        </div>
                    </div>
                </div>
            </>
        )
    }

    renderDisplay(): JSX.Element {
        return (<>
            <div className="form-group col-sm-6">{this.model.date && <div className="row">{this.getResource('GL_DATE_L') + ": " + this.dateDisplayFor(this.model.date, null, this.getResource('GL_YEAR_ABBREVIATION_L'))}</div>}
                {this.model.processingRenewDistraintRequired ? <div className="row"> {this.getResource('CR_APP_RENEWAL_ENTRY_L')} </div> : null} </div>
        </>);
    }
}

export const F224_PledgeRenewDateFieldUI = withFieldRecordContainer(F224_PledgeRenewDateUI, {
    fieldLabelTextKey: "CR_F_224_L",
    fieldLabelFor: (model: F224_PledgeRenewDate) => model.date
});