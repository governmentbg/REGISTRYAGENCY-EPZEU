import { EPZEUBaseComponent } from 'EPZEU.Core';
import { FieldContainerProps, withFieldRecordContainer } from 'EPZEU.CR.Domain';
import * as React from "react";
import { F313_Term } from '../../Models/Fields/ModelsAutoGenerated';

class F313_TermUI extends EPZEUBaseComponent<FieldContainerProps, F313_Term> {

    renderEdit(): JSX.Element {
        return (
            <div className="row">
                <div className="form-group col-sm-6">
                    {this.dateFor(x => x.date)}
                </div>
            </div>
        );
    }

    renderDisplay(): JSX.Element {
        return (<>{this.model.date && <>{this.dateDisplayFor(this.model.date, null, this.getResource('GL_YEAR_ABBREVIATION_L'))}</>}</>);
    }
}

export const F313_TermFieldUI = withFieldRecordContainer(F313_TermUI, {
    fieldLabelTextKey: "CR_F_313_L",
    fieldLabelFor: (model: F313_Term) => model.date
});