import { BaseProps } from 'Cnsys.UI.React';
import { EPZEUBaseComponent, ValidationSummaryErrorsPreviewUI } from 'EPZEU.Core';
import { withFieldRecordContainer } from 'EPZEU.CR.Domain';
import { observer } from 'mobx-react';
import * as React from 'react';
import { F210_SecuredClaimInterests } from '../../Models/Fields/ModelsAutoGenerated';



@observer class F210_SecuredClaimInterestsUI extends EPZEUBaseComponent<BaseProps, F210_SecuredClaimInterests> {

    renderEdit() {
        return (
            <div className="row">
                <div className="form-group col-sm-6 col-lg-4">
                    <div className="form-row">
                        <div className="col">
                            {this.textBoxFor(m => m.text)}
                        </div>
                        <div className="col-auto">
                            <div className="col-form-label"> {this.getResource("GL_PERCENT_ABBRAVETION_L")} </div>
                        </div>
                    </div>
                </div>
            </div>
        );
    }

    renderDisplay(): JSX.Element {
        return (<>
            {this.model.text} {this.getResource("GL_PERCENT_ABBRAVETION_L")}
            <ValidationSummaryErrorsPreviewUI errors={this.props.modelReference.getErrors()} />
        </>);
    }
}

export const F210_SecuredClaimInterestsUIFieldUI = withFieldRecordContainer(F210_SecuredClaimInterestsUI, {
    fieldLabelTextKey: "CR_F_210_L",
    fieldLabelFor: (x: F210_SecuredClaimInterests) => x.text,
});