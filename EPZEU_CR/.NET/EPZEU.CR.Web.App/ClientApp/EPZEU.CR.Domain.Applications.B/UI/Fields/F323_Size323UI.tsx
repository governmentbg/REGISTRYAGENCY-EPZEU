import * as React from "react";
import { EPZEUBaseComponent } from 'EPZEU.Core'
import { F323_Size323 } from '../../Models/Fields/ModelsAutoGenerated'
import { withFieldRecordContainer, RecordContainerProps } from 'EPZEU.CR.Domain';

class F323_Size323UI extends EPZEUBaseComponent<RecordContainerProps, F323_Size323>{

    renderEdit(): JSX.Element {
        return (<>
            <div className="row">
                <div className="form-group col-sm-6 col-lg-4">
                    {this.labelFor(m => m.price.amount, 'CR_APP_SIZE_L')}
                    {this.textBoxFor(m => m.price.amount)}
                </div>
                <div className="form-group col-sm-6 col-lg-4">
                    {this.labelFor(m => m.price.units, 'CR_APP_CURRENCY_L')}
                    {this.textBoxFor(m => m.price.units)}
                </div>
            </div>
        </>)
    }

    renderDisplay(): JSX.Element {
        return (<>
            {this.model.price.amount && <>{this.model.price.amount + " "}</>}
            {this.model.price.units && <>{this.model.price.units}</>}
        </>);
    }
}


export const F323_Size323FieldUI = withFieldRecordContainer(F323_Size323UI,
    {
        fieldLabelTextKey: "CR_F_323_L",
    });
