import { EPZEUBaseComponent } from 'EPZEU.Core';
import { ApplicationFormContextProviderProps, FieldContainerProps, ProcessStates, withApplicationFormContext, withFieldRecordContainer } from 'EPZEU.CR.Domain';
import { observer } from "mobx-react";
import * as React from "react";
import { F044_EraseProcura } from '../../Models/Fields/ModelsAutoGenerated';

interface F044_EraseProcuraProps extends FieldContainerProps, ApplicationFormContextProviderProps {
}

@observer class F044_EraseProcuraUI extends EPZEUBaseComponent<F044_EraseProcuraProps, F044_EraseProcura> {

    renderEdit(): JSX.Element {
        return (
            <div className="row">
                <div className="form-group col">
                    <div className="custom-control custom-checkbox">
                        {this.checkBoxFor(x => x.cheked, "GL_EFFACEMENT_L",
                            { className: "custom-control-input", disabled: this.props.applicationManager.processState == ProcessStates.ForChange && this.model.initialState.cheked === true ? true : false })}
                    </div>
                </div>
            </div>
        );
    }

    renderDisplay(): JSX.Element {
        return < div > <>{(this.model.cheked ? this.getResource('GL_OK_L') : "")}</></div >
    }
}

export const F044_EraseProcuraFieldUI = withFieldRecordContainer(withApplicationFormContext(F044_EraseProcuraUI), { fieldLabelTextKey: "CR_F_44_L" });