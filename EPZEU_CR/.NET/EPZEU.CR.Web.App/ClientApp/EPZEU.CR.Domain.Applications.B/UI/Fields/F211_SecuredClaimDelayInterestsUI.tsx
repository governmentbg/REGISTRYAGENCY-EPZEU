import { BaseProps } from 'Cnsys.UI.React';
import { EPZEUBaseComponent, ValidationSummaryErrorsPreviewUI } from 'EPZEU.Core';
import { withFieldRecordContainer } from 'EPZEU.CR.Domain';
import { observer } from 'mobx-react';
import * as React from 'react';
import { F211_SecuredClaimDelayInterests } from '../../Models/Fields/ModelsAutoGenerated';



@observer class F211_SecuredClaimDelayInterestsUI extends EPZEUBaseComponent<BaseProps, F211_SecuredClaimDelayInterests> {

    renderEdit() {
        return (
            <div className="row">
                <div className="form-group col-sm-6 col-lg-4">
                    {this.textBoxFor(m => m.text)}
                </div>
            </div>
        );
    }

    renderDisplay(): JSX.Element {
        return (<>
            {this.model.text}
            <ValidationSummaryErrorsPreviewUI errors={this.props.modelReference.getErrors()} />
        </>);
    }
}

export const F211_SecuredClaimDelayInterestsUIFieldUI = withFieldRecordContainer(F211_SecuredClaimDelayInterestsUI, {
    fieldLabelTextKey: "CR_F_211_L",
    fieldLabelFor: (x: F211_SecuredClaimDelayInterests) => x.text,
});