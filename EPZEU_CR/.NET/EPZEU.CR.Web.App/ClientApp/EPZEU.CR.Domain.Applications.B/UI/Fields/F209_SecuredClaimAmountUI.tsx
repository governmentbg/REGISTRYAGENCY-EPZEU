import * as React from "react";
import { EPZEUBaseComponent } from 'EPZEU.Core'
import { F209_SecuredClaimAmount } from '../../Models/Fields/ModelsAutoGenerated'
import { withFieldRecordContainer, FieldContainerProps } from 'EPZEU.CR.Domain';
import { ObjectHelper } from "Cnsys.Core";

class F209_SecuredClaimAmountUI extends EPZEUBaseComponent<FieldContainerProps, F209_SecuredClaimAmount> {

    renderEdit(): JSX.Element {
        return <div className="row">
            <div className="form-group col-sm-6 col-lg-4">
                {this.labelFor(x => x.amountDue, "CR_APP_AMOUNT_DUE_L")}
                {this.textBoxFor(x => x.amountDue)}
            </div>
            <div className="form-group col-sm-6 col-lg-4">
                {this.labelFor(x => x.units, "CR_APP_UNITS_L")}
                {this.textBoxFor(x => x.units)}
            </div>
        </div>
    }

    renderDisplay(): JSX.Element {
        return (<div>
            <div>{!ObjectHelper.isArrayNullOrEmpty(this.model.amountDue) ? <>{this.getResource("CR_APP_AMOUNT_DUE_L") + ": " + this.model.amountDue}</> : null}</div>
            <div>{!ObjectHelper.isArrayNullOrEmpty(this.model.units) ? <>{this.getResource("CR_APP_UNITS_L") + ": " + this.model.units}</> : null}</div>
        </div>);
    }
}

export const F209_SecuredClaimAmountFieldUI = withFieldRecordContainer(F209_SecuredClaimAmountUI, {
    fieldLabelTextKey: "CR_F_209_L",
    isMandatoryField: true
});