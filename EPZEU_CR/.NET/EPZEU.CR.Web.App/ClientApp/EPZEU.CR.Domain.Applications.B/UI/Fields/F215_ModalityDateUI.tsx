import { EPZEUBaseComponent } from 'EPZEU.Core';
import { FieldContainerProps, withFieldRecordContainer } from 'EPZEU.CR.Domain';
import * as React from "react";
import { F215_ModalityDate } from '../../Models/Fields/ModelsAutoGenerated';

class F215_ModalityDateUI extends EPZEUBaseComponent<FieldContainerProps, F215_ModalityDate> {

    renderEdit(): JSX.Element {
        return (<div className="row">
            <div className="form-group col-sm-6">
                {this.dateFor(x => x.date)}
            </div>
        </div>)
    }

    renderDisplay(): JSX.Element {
        return (<>{this.model.date && <>{this.dateDisplayFor(this.model.date, null, this.getResource('GL_YEAR_ABBREVIATION_L'))}</>}</>);
    }
}

export const F215_ModalityDateFieldUI = withFieldRecordContainer(F215_ModalityDateUI, {
    fieldLabelTextKey: "CR_F_215_L",
    fieldLabelFor: (model: F215_ModalityDate) => model.date
});