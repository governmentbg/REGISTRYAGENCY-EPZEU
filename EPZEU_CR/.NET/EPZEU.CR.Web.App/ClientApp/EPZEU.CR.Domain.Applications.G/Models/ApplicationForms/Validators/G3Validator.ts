import { ApplicationFormGValidator, ApplicationFormGFieldsBaseValidator } from './ApplicationFormGValidator';
import { G3 } from '../../ApplicationForms/ApplicationFormsG';
import { G3Fields } from '../ModelsAutoGenerated';

export class G3Validator extends ApplicationFormGValidator<G3> {
    constructor() {
        super();
    }

    public validate(obj: G3): boolean {
        let isValid = super.validate(obj);

        return isValid;
    }
}

export class G3FieldsValidator extends ApplicationFormGFieldsBaseValidator<G3Fields>{
    constructor() {
        super();
    }

    public validate(obj: G3Fields): boolean {
        let isValid = super.validate(obj);

        return isValid;
    }
}