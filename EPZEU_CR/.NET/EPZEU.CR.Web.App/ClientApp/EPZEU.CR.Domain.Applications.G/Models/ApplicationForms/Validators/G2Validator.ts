import { ApplicationFormGValidator, ApplicationFormGFieldsBaseValidator } from './ApplicationFormGValidator';
import { G2 } from '../../ApplicationForms/ApplicationFormsG';
import { G2Fields } from '../ModelsAutoGenerated';

export class G2Validator extends ApplicationFormGValidator<G2> {
    constructor() {
        super();
    }

    public validate(obj: G2): boolean {
        let isValid = super.validate(obj);

        return isValid;
    }
}

export class G2FieldsValidator extends ApplicationFormGFieldsBaseValidator<G2Fields>{
    constructor() {
        super();
    }

    public validate(obj: G2Fields): boolean {
        let isValid = super.validate(obj);

        return isValid;
    }
}