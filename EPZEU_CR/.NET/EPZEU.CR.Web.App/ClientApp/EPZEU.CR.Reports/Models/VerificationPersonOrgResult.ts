import { observable } from 'mobx';
import { TypeSystem, BaseDataModel } from 'Cnsys.Core';
import { moduleContext } from '../ModuleContext';
import { PhysicalOrCompanyObj } from './ModelsAutoGenerated';

@TypeSystem.typeDecorator('VerificationPersonOrgResult', moduleContext.moduleName)
export class VerificationPersonOrgResult extends BaseDataModel {

    @observable private _items: PhysicalOrCompanyObj[] = null;

    constructor(obj?: any) {
        super(obj)

        this.copyFrom(obj);
    }

    @TypeSystem.propertyArrayDecorator(PhysicalOrCompanyObj ? PhysicalOrCompanyObj : moduleContext.moduleName + '.' + 'PhysicalOrCompanyObj')
    public set items(val: PhysicalOrCompanyObj[]) {
        this._items = val;
    }

    public get items(): PhysicalOrCompanyObj[] {
        return this._items;
    }
}