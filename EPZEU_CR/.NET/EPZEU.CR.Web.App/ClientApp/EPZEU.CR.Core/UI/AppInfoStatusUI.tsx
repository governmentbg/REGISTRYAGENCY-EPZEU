import * as React from "react";
import { ApplicationInfo } from "../Models/ModelsAutoGenerated";

export interface AppInfoStatusUIProps {
    data: ApplicationInfo;    
}

export const AppInfoStatusUI = (props:AppInfoStatusUIProps) => {
    return <span dangerouslySetInnerHTML={{ __html: props.data.resultHTML }}></span>;
}
